<!DOCTYPE html>
<html lang="en">
{{> head}}
<body>
  {{> nav}}
  <div class="columns">
    <div class="column is-three-quarters">
      <section class="section">
        <div class="container content">
          <h1>{{metadata.title}}</h1>
          <h3>By {{metadata.author}} - {{metadata.date}}</h2>
          {{{content}}}
        </div>
      </section>
    </div>
    <div class="column">
      {{> article-menu}}
    </div>
  </div>
  {{> footer}}
</body>
</html>